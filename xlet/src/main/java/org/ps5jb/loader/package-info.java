/**
 * Main package for the Loader Xlet. Note that the JAR received for execution should not use the same package name
 * for its classes. Otherwise, the loader will throw an exception attempting to load the remote JAR.
 */
package org.ps5jb.loader;